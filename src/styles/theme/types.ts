export type Theme = {
  font: {
    family: string;
    sizes: {
      xs: string;
      sm: string;
      md: string;
    };
  };
  colors: {
    black: string;
    black_two: string;
    header: string;
    background: string;
    primary: string;
    font: string;
    disabled: string;
    placeholder: string;
    warm_grey: string;
    greyish_brown: string;
  };
};

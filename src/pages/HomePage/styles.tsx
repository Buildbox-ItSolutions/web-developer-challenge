import styled from 'styled-components';

export const Container = styled.div`
    background-color: var(--black);
    position: absolute;
    top: 0;
    height: 100%;
    width: 100%;
    overflow-y: auto;
`;
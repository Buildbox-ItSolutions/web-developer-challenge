import React from 'react'
import { render } from '@testing-library/react'
import PublicButton from '.'

describe('<PublicButton />', () => {
  it('dummy test', () => {
    render(<PublicButton />)
  })
})

import { Header } from "../Header"
import { Post } from "../Post"
import { Wrapper } from "./style"

export const Main = () => {
  return (
    <Wrapper>
      <Header />
      <Post />
    </Wrapper>
  )
}
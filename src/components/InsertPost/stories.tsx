import { Story, Meta } from '@storybook/react'
import InsertPost from '.'

export default {
  title: 'InsertPost',
  component: InsertPost
} as Meta

export const Default: Story = () => <InsertPost />

import styled from 'styled-components';

export const Container = styled.header`
    background: var(--color-header);
    height: 93px;
    text-align: center;
    position: fixed;
    width: 100%;
    z-index: 100;
    top: 0;

    img{
        padding: 24px 0;
    }
`;

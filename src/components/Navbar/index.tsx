import { NavbarContainer, NavbarText, Subtitle } from './styled';

const Navbar = () => (
  <NavbarContainer>
    <NavbarText>BuildBox</NavbarText>
    <Subtitle>WEB CHALLENGE</Subtitle>
  </NavbarContainer>
);

export default Navbar;
